# Schema Migration: 001_create_conversation_state.sql

Phase: 2B2B2 -- Create and Verify the Lakebase Conversation-State Schema
Date: 2026-07-18
Status: APPLIED (committed to databricks_postgres)

## Migration file

`migrations/lakebase/001_create_conversation_state.sql`

## Objects created

| Object | Type | Details |
|---|---|---|
| `transparence_state` | Schema | Owned by lokesh.choraria@te.com |
| `transparence_state.app_conversation` | Table | 10 columns, 6 constraints |
| `idx_app_conversation_owner_updated` | Index | `(owner_user_id_hash, updated_at DESC)` |
| `idx_app_conversation_genie_conv` | Index (partial) | `(genie_conversation_id) WHERE NOT NULL` |
| `idx_app_conversation_status_last_active` | Index | `(status, last_active_at)` |

## Column definition

| Position | Column | Type | Nullable | Notes |
|---|---|---|---|---|
| 0 | `conversation_id` | UUID | NOT NULL | PK; generated by adapter |
| 1 | `owner_user_id_hash` | TEXT | NOT NULL | HMAC-SHA256 of X-Forwarded-User |
| 2 | `frontend_conversation_id` | TEXT | NOT NULL | Browser-supplied UUID |
| 3 | `genie_conversation_id` | TEXT | NULL | NULL until first Genie turn |
| 4 | `last_genie_message_id` | TEXT | NULL | NULL until first Genie turn |
| 5 | `status` | TEXT | NOT NULL | ACTIVE/STALE/RESET/EXPIRED |
| 6 | `version` | INTEGER | NOT NULL | Optimistic concurrency counter |
| 7 | `created_at` | TIMESTAMPTZ | NOT NULL | Immutable after INSERT |
| 8 | `updated_at` | TIMESTAMPTZ | NOT NULL | Last mutation time |
| 9 | `last_active_at` | TIMESTAMPTZ | NOT NULL | Last user-activity time |

Column order matches `_COLUMNS` tuple in
`app/services/lakebase_conversation_repository.py` exactly.
Row mapper is positional (row[0]..row[9]).

## Constraints

| Name | Type | Definition |
|---|---|---|
| `pk_app_conversation` | PRIMARY KEY | `(conversation_id)` |
| `uq_app_conversation_owner_frontend` | UNIQUE | `(owner_user_id_hash, frontend_conversation_id)` |
| `chk_app_conversation_status` | CHECK | `status IN ('ACTIVE','STALE','RESET','EXPIRED')` |
| `chk_app_conversation_version` | CHECK | `version >= 1` |
| `chk_app_conversation_updated_at` | CHECK | `updated_at >= created_at` |
| `chk_app_conversation_last_active_at` | CHECK | `last_active_at >= created_at` |

## Execution method

Single psycopg3 transaction via `executeCode` (Python) with OAuth token from
`w.postgres.generate_database_credential()`. Committed after all 11
in-transaction verification checks passed. Post-commit verification passed on
fresh connection.

## Platform constraints discovered

See `ownership_and_permissions.md` for full details.
